[package]
name = "ioutgt-uring"
description = "Per-thread io_uring reactor and op futures integrated with a Tokio current-thread runtime"
version.workspace = true
publish.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
io-uring = { workspace = true }
libc = { workspace = true }
slab = { workspace = true }
tokio = { workspace = true, features = ["rt"] }

[dev-dependencies]
tempfile = { workspace = true }

[features]
## Expose internal test-only counters for integration tests in sibling crates.
## Never enable in production code.
test-helpers = []

[lints]
workspace = true
