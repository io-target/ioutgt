[package]
name = "ioutgt-stream"
description = "Protocol-neutral ZC-aware gather-send harness shared by stream transports (NVMe/TCP, NBD)"
version.workspace = true
publish.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
ioutgt-core = { workspace = true }
ioutgt-uring = { workspace = true }
libc = { workspace = true }

[dev-dependencies]
# Enable internal test counters for the ring reader tests
# (ring_drain_calls_recv_done_not_provide).
ioutgt-uring = { path = "../ioutgt-uring", features = ["test-helpers"] }

[lints]
workspace = true
