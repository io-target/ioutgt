/target
**/*.rs.bk
*.pcap
perf.data*
flamegraph*.svg
/bench/results/
/.codegraph/
/docs/superpowers/
*.swp
# Local scratch configs (nvmetcli, spdk, ...) stay out of the tree.
*.conf
*~
# vmtest VM images + host<->guest markers (see testing/common/vmtest.sh)
/testing/vmtest/data/

# Harness-managed git worktrees (EnterWorktree) live inside the checkout.
/.claude/worktrees/
